// Blazor JS initializer for DExpressClone.Components

export function beforeStart(options, extensions) {
    // Called before Blazor starts. Use for early initialization if needed.
}

export function afterStarted(blazor) {
    // Called after Blazor has fully started. Use for post-initialization if needed.
}
